#include "main.h"
/**
 * add - Entry point
 *
 * @a: is the number to add and check
 * @b: is the number to add and check
 *
 * Return: Always 0
 */
int add(int a, int b)
{
	int z;

	for (a > 0 && b > 0)
{
	z = a + b;
	_putchar(z);
	return (z);
}
}
