#include "main.h"
/**
 * add - Entry point
 *
 * @a: is the number to add and check
 * @b: is the number to add and check
 *
 * Return: Always 0
 */
int add(int a, int b)
{

	_putchar(a + b);
	return (z);
}
