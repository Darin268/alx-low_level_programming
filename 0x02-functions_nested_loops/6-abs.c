#include "main.h"
/**
 * _abs - Entry point
 *
 * @n: is the number to check
 *
 * Return: 0 Always (Success)
 */
int int _abs(int n)
{
	int y;

	if (n < 0)
	{
		y = n * -1;
		return (y);
}
return (n);
}
