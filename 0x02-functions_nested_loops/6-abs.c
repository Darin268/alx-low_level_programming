#include "main.h"
/**
 * _abs - Entry point
 *
 * @x: is the number to check
 * @y: is absolute value of x
 *
 * Return: 0 Always (Success)
 */
int _abs(int x)
{
	int y;

	y = _abs(x);
	_putchar(y);
	return (0);
}
