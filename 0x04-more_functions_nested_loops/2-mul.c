#include "main.h"

/**
 *
 * mul - Entry point
 *
 * @a: first character to multiply
 * @b: second character to multiply
 *
 * Return: Always 0 (Success)
 */

int mul(int a, int b)
{
	int x;
	
	x = a * b;

	return (x);
}
