#include "main.h"

/**
 * mul - a function to multiply two digits
 *
 * @a: first character to multiply
 * @b: second character to multiply
 * Return: to answer
 */

int mul(int a, int b)
{
	int x;

	x = a * b;

	return (x);
}
